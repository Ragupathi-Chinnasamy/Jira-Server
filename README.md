# Task-tracker

To install dependencies:

```bash
npm install
```

To generate prisma schema:

```bash
npx prisma generate
```

To run:

```bash
npm run start
```

To run in watch mode:

```bash
npm run start:dev
```